# SYSTEM Playbook

## Token roles (not raw hex)
bg/surface/textPrimary/textSecondary/border/accent/danger/warning/success

## Steps
1) Define tokens + naming + theming (light/dark)
2) Build core components (buttons, inputs, nav, feedback, overlays)
3) Document states + a11y + usage rules
4) Enforce with linting/code review rules (no raw values)
5) Version + changelog + migration notes

## Output
- Token table
- Component inventory + states
- Governance plan
